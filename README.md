# Дослідження методу Градієнтного Спуску (ГС) для розв'язання задачі мультилатерації (TDoA) методом Найменших Квадратів (МНК).

## Мета
1. Практично реалізувати та дослідити роботу алгоритму градієнтного спуску.
2. Зрозуміти суть Методу Найменших Квадратів (МНК) як способу формулювання функції втрат (loss function).
3. Візуалізувати та проаналізувати ключові параметри оптимізації: швидкість навчання (learning rate), вплив шуму (noise level) та початкове припущення (initial guess).
4. Дослідити інженерні трейдофи та фундаментальні проблеми реальних систем, такі як Геометричне Послаблення Точності (GDOP).

# Експериментальні Завдання

## 1. Контрольний Запуск ("Ідеальний світ")

### Налаштування:

- NOISE_LEVEL = 0 (повна відсутність шуму)
- LEARNING_RATE = 0.01
- INITIAL_GUESS = [50000, 50000]
- TRUE_POSITION = [45000, 35000]

<img width="1002" height="770" alt="image" src="https://github.com/user-attachments/assets/abaa03fc-29d6-4c72-ad14-ac6882c7a67c" />

### Аналіз та Контрольні Питання:

  Фінальна похибка позиціонування: 0.00 метрів. 
  Це означає, що після округлення до двох знаків результат збігся з реальною позицією. За ідеальних даних без шуму градієнтний спуск успішно знайшов точку мінімуму функції втрат.
  Може все ж бути невелика похибка через те, що алгоритм завершується не тоді, коли точка ідеальна, а коли зміна функції втрат між ітераціями стала меншою за поріг: delta_loss < 1e-7

Графік величини градієнта показує, як змінюється швидкість наближення алгоритму до оптимального розв’язку. На початкових ітераціях значення градієнта є великим, оскільки стартова точка знаходиться далеко від мінімуму функції втрат, тому алгоритм робить значні кроки та швидко змінює координати. У процесі наближення до правильної позиції градієнт поступово зменшується, що свідчить про зниження нахилу функції втрат і зменшення необхідності великих коригувань. Наприкінці роботи алгоритму значення градієнта наближається до нуля, тобто система досягає точки мінімуму, де подальші зміни координат майже не покращують результат. Це безпосередньо пов’язано з графіком Loss: на початку, коли градієнт великий, функція втрат зменшується швидко, а ближче до мінімуму, коли градієнт стає малим, зниження Loss сповільнюється.


## 2. Дослідження learning_rate ("Проблема Золотоволоски")

### Налаштування: Використовуйте базові налаштування з Завдання 1, але поверніть NOISE_LEVEL = 1e-6. Послідовно змініть тільки LEARNING_RATE.

### Експеримент 2a (Розбіжність): LEARNING_RATE = 2.0

<img width="1025" height="726" alt="image" src="https://github.com/user-attachments/assets/b65c4a3a-61da-491f-a91d-f1dc7500f93b" />

У експерименті 2a при значенні LEARNING_RATE = 2.0 алгоритм працює нестабільно, оскільки крок оновлення параметрів є занадто великим. На 2D-графіку шляху оптимізації видно хаотичну траєкторію з великими стрибками координат, а знайдена позиція виявилась дуже далеко від справжньої точки, що підтверджується великою похибкою позиціонування. Таку поведінку називають розбіжністю (divergence) - ситуація, коли ітераційний метод не наближається до правильного розв’язку, а навпаки віддаляється від нього або безкінечно коливається навколо мінімуму через невдало вибраний параметр навчання.

### Експеримент 2b (Стагнація): LEARNING_RATE = 1e-5

<img width="1035" height="741" alt="image" src="https://github.com/user-attachments/assets/5be3dcac-549c-4184-ac42-d81c6f6db9d6" />

### Експеримент 2c (Оптимальний): LEARNING_RATE = 0.01

<img width="1060" height="764" alt="image" src="https://github.com/user-attachments/assets/0d5d7d25-942c-4627-9201-fd0a02a4c201" />

В експерименті 2b при LEARNING_RATE = 1e-5 алгоритм не досяг умови збіжності за встановлену кількість MAX_ITERATIONS = 100000, хоча поступово наближався до правильного розв’язку. Це видно з графіка Loss, де функція втрат повільно та майже лінійно зменшується, але не встигає дійти до мінімуму за доступну кількість ітерацій. Отримана позиція є відносно близькою до справжньої, однак похибка все ще становить сотні метрів, що свідчить про недостатню швидкість навчання. У порівнянні з експериментом 2c при LEARNING_RATE = 0.01, де збіжність була досягнута за 430 ітерацій, крива навчання для 2b спадає значно повільніше, а графік величини градієнта також зменшується дуже повільними темпами. Така поведінка називається стагнацією (stagnation) - ситуацією, коли алгоритм формально рухається в правильному напрямку, але робить настільки малі кроки, що практично “завмирає” і потребує надто багато часу для досягнення мінімуму.

learning_rate є найважливішим гіперпараметром градієнтного спуску, оскільки саме він визначає розмір кожного кроку під час оновлення параметрів. Якщо крок занадто великий, алгоритм перескакує через мінімум і стає нестабільним. Якщо крок занадто малий, оптимізація відбувається надто повільно.

## 3. Дослідження NOISE_LEVEL ("Сміття на вході — сміття на виході")

### Налаштування: Використовуйте базові налаштування, але послідовно змінюйте NOISE_LEVEL.

### Експеримент 3a (Високоточний): NOISE_LEVEL = 1e-9 (1 нс)

<img width="1046" height="799" alt="image" src="https://github.com/user-attachments/assets/cfc0cca1-52f4-4ca6-b79a-8bffa5ef00d4" />

### Експеримент 3b (Реалістичний): NOISE_LEVEL = 1e-6 (1 мкс)

<img width="1039" height="789" alt="image" src="https://github.com/user-attachments/assets/4092218a-6030-40b3-bbd6-34eecd6e2fea" />

### Експеримент 3c (Зламаний): NOISE_LEVEL = 1e-4 (100 мкс)

<img width="1028" height="776" alt="image" src="https://github.com/user-attachments/assets/494d18c7-57ae-44e1-924b-1e9fe01950dd" />


| NOISE_LEVEL (с) | NOISE (м) | Фінальна похибка (м) |
| --- | --- | --- |
| 1e-9 | 0.30 м | 0.36 м |
| 1e-6 | 300 м | 214.22 м |
| 1e-4 | 30 000 м | 3917.99 м |


З отриманих результатів видно пряму залежність між рівнем шуму у вхідних даних та точністю знайденої координати. При дуже малому шумі 1e-9 секунд похибка становить лише 0.36 м, тобто система працює майже ідеально. Коли шум збільшується до 1e-6 секунд, це вже приблизно 300 метрів похибки у вимірі відстані, тому кінцева позиція зміщується на 214.22 м. При шумі 1e-4 секунд помилка вхідних вимірювань сягає майже 30 км, фінальна похибка 3917.99 м.

У експерименті 3c алгоритм усе одно сходиться, тому що градієнтний спуск не шукає “справжню координату”, а мінімізує функцію втрат для тих даних, які йому подали. Якщо вхідні TDoA сильно зашумлені, то алгоритм коректно знаходить точку, яка найкраще узгоджується саме з помилковими вимірюваннями. Тобто графік Loss падає, бо математична задача оптимізації розв’язується успішно, але розв’язується вона для неправильних даних.

## 4. Дослідження INITIAL_GUESS ("Вплив початкової точки")

### Налаштування: Використовуйте базові налаштування (NOISE_LEVEL = 1e-6), але змінюйте INITIAL_GUESS.

### Експеримент 4a (Добре): INITIAL_GUESS = [50000, 50000]
<img width="827" height="694" alt="image" src="https://github.com/user-attachments/assets/1aaf0591-a755-4d93-91e8-21172051c0b8" />

### Експеримент 4b (Погане): INITIAL_GUESS = [0, 0]
<img width="837" height="699" alt="image" src="https://github.com/user-attachments/assets/38a7e3a4-5743-478d-89fc-e89465ebb880" />

### Експеримент 4c (Дуже погане): INITIAL_GUESS = [-500000, -500000]
<img width="849" height="687" alt="image" src="https://github.com/user-attachments/assets/dd7c1ca0-d423-43fb-b3ba-4b2606726d1f" />

У проведених експериментах початкова точка вплинула на швидкість збіжності алгоритму. Для випадку 4a з початковим наближенням [50000, 50000], яке знаходиться близько до справжньої позиції [45000, 35000], алгоритм досяг збіжності за 434 ітерації. Для випадку 4c з дуже далеким стартом [-500000, -500000] знадобилося 51448 ітерацій. Це означає, що чим далі початкова точка від оптимального розв’язку, тим довше градієнтному спуску потрібно рухатися поверхнею функції втрат до мінімуму.

Початкове припущення зазвичай більше впливає на швидкість збіжності, ніж на фінальну точність. Якщо функція втрат має форму однієї “чаші” з єдиним мінімумом, то алгоритм із різних стартових точок рухається до одного й того самого розв’язку.

## 5. Дослідження Геометрії ("Прокляття плаского каньйону" - GDOP)

### Налаштування: Використовуйте базові налаштування (NOISE_LEVEL = 1e-6), але змініть положення цілі.

### Експеримент 5a (Хороша геометрія): TRUE_POSITION = [45000, 35000] (ціль всередині трикутника станцій).
<img width="865" height="697" alt="image" src="https://github.com/user-attachments/assets/9ec27abe-2627-4e3c-a777-591d3125ee4a" />

### Експеримент 5b (Погана геометрія): TRUE_POSITION = [45000, -135000] (ціль далеко збоку від усіх станцій).
<img width="854" height="669" alt="image" src="https://github.com/user-attachments/assets/7cff53c7-ec4b-45d0-919f-f8322773d654" />

У експерименті 5a фінальна похибка становила приблизно 186 м, а у 5b близько 8339 м, хоча рівень шуму в обох випадках був однаковий (1e-6). Причина полягає не в шумі, а в геометрії розташування станцій відносно цілі. У випадку 5a ціль знаходиться всередині трикутника базових станцій, тому перетин гіпербол TDoA відбувається під хорошими кутами. У випадку 5b ціль розташована далеко збоку від усіх станцій, тому лінії перетину майже паралельні. Через це невелика помилка у вимірюваннях часу перетворюється на велику помилку в координатах.

На графіку Delta Loss для 5b видно, що зміни функції втрат ставали дедалі меншими, тобто алгоритм майже перестав покращувати результат. Він зупинився не тому, що знайшов точну позицію, а тому що потрапив у область, де поверхня функції втрат стала дуже пласкою. Градієнт там малий, тому кожен новий крок майже не змінює значення Loss. Алгоритм бачить, що покращення майже немає, тому завершує роботу, хоча координата ще далеко від істинної точки.

## 6 (Бонус). Дослідження TOLERANCE ("Ціна точності")

### Налаштування: Використовуйте "погану" геометрію з Завдання 5b (TRUE_POSITION = [45000, -135000]). Змініть TOLERANCE.

### Експеримент 6a (Швидко і брудно): TOLERANCE = 1e-2
<img width="831" height="682" alt="image" src="https://github.com/user-attachments/assets/9d172d5d-aedb-4ec8-91b6-d80dd192ff00" />

### Експеримент 6b (Базовий): TOLERANCE = 1e-7
<img width="838" height="690" alt="image" src="https://github.com/user-attachments/assets/9078725e-dfbb-4532-b73e-86a196c57f6c" />

### Експеримент 6c (Надточний): TOLERANCE = 1e-12 (також збільште MAX_ITERATIONS до 100000, щоб дати йому час)
<img width="834" height="688" alt="image" src="https://github.com/user-attachments/assets/a83b3566-e361-4e52-a6a7-9893c5737acd" />

| К-сть ітерацій | Фінальна Похибка |
| --- | --- |
| максимально | 3785.31 м |
| максимально | 3249.91 м |
| максимально | 126.31 м |

У трьох експериментах видно прямий компроміс між швидкістю роботи та точністю результату. Для 6a (TOLERANCE = 1e-2) алгоритм завершився швидше, але фінальна похибка залишилась близько 3785 м, а значення Loss було відносно великим. Для 6b (1e-7) результат став кращим: похибка зменшилась приблизно до 3249 м. Для 6c (1e-12) після тих самих 100000 ітерацій похибка вже становила лише 126 м, тобто значно покращилась, хоча алгоритм усе одно не досяг формального критерію збіжності.

Чи варто було платити додатковими десятками тисяч ітерацій за перехід від 1e-7 до 1e-12? У цьому запуску так, бо похибка зменшилась з кількох кілометрів до сотень метрів. Але загалом це не завжди вигідно, приріст точності може бути незначним, а час обчислень великим.

## Висновок
На точність і швидкість роботи градієнтного спуску найбільше впливають чотири основні фактори: learning rate, якість вхідних даних (noise level), початкове наближення та геометрія розташування базових станцій. 

Найважливішим параметром для швидкості збіжності є learning rate, оскільки занадто велике значення спричиняє нестабільність або розбіжність алгоритму, а занадто мале дуже повільне навчання та стагнацію. 

На точність результату найбільше впливає рівень шуму у вхідних вимірюваннях. Чим більша похибка у TDoA-даних, тим менш точно алгоритм може визначити реальні координати. Також критично важливою є геометрія базових станцій (GDOP): при хорошому розташуванні станцій шумні дані дають прийнятний результат, а при поганому невеликий шум може викликати багатокілометрову похибку.

Початкове наближення переважно впливає на кількість ітерацій, необхідних для збіжності. Якщо стартова точка близька до оптимального розв’язку, алгоритм працює швидше. Якщо ж вона далека, зростають витрати часу та ризик повільної збіжності.
